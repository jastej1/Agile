<% layout('../layouts/mainlayout.ejs') -%>
<br />

<link rel='stylesheet' href='/public/formstyle.css' />

<div class="login-form">
    <form action="/user/UpdateUser" method="POST">
        <input id="old_username" name="old_username" type="hidden" value=<%=reqInfo.username %> />
        <h2 class="text-center">Update Profile</h2>
        <img style="display:block; margin-left: auto; margin-right: auto; padding-bottom: 20px;"
            src="/public/usericon.png" width="100px">
        <div class="form-group">
            <input name="username" class="form-control" type="text" placeholder="Username"
                value="<%= user.username ? user.username : '' %>">
        </div>
        <div class="form-group">
            <input name="email" class="form-control" type="text" placeholder="Email"
                value="<%= user.email ? user.email : '' %>">
        </div>
        <div class="form-group">
            <input name="firstName" class="form-control" type="text" placeholder="First Name"
                value="<%= user.firstName ? user.firstName : '' %>">
        </div>
        <div class="form-group">
            <input name="lastName" class="form-control" type="text" placeholder="Last Name"
                value="<%= user.lastName ? user.lastName : '' %>">
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-primary btn-block">Update</button>
        </div>
    </form>
</div>
<%= errorMessage %><br />